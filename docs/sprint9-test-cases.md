# Sprint 9 测试用例 — URL 分析模式 & 历史页面落地

> **QA Agent:** robot01  
> **测试基线:** 2026-06-30  
> **覆盖用户故事:** US-47 (URL 分析模式), US-HistoryPage (/history 独立页面)  
> **基线代码:** frontend/src/components/InputSection.jsx, frontend/src/pages/History.jsx, frontend/src/components/HistoryPanel.jsx, backend/app/api/routes.py, backend/app/api/schemas.py  

---

## 一、基线代码审查（Sprint 9 前快照）

### 1.1 URL 分析 — 前端现状

| 位置 | 关键发现 |
|------|---------|
| `InputSection.jsx:103-121` | URL Tab 已存在，但 `Input` 组件 `disabled`，下方显示蓝色 "即将上线" 提示 |
| `InputSection.jsx:247` | `activeTab === 'manual'` 时渲染提交按钮；**无 URL Tab 的提交按钮** |
| `tabItems` | `url` / `file` / `multi-source` / `manual` 四个 Tab，顺序为 URL 优先 |

**US-47 需求：** URL Tab 变为可用状态——Input 可输入 URL，添加提交按钮，调用后端 URL 分析 API，展示分析结果并保存到历史。

### 1.2 URL 分析 — 后端现状

| 位置 | 关键发现 |
|------|---------|
| `routes.py` | 无 `/analyze/url` 端点 |
| `schemas.py` | 无 URL 分析请求/响应 Schema |
| `ai_service.py` | 需确认是否有 `analyze_url` 函数（Service 层） |

**US-47 需求：** 新增 `POST /analyze/url` 端点，接受 URL + 可选参数，调用 AI 服务分析目标 URL 的漏洞面，返回结构化结果，保存到 MySQL + Redis 缓存。

### 1.3 历史页面 — 前端现状

| 位置 | 关键发现 |
|------|---------|
| `History.jsx` | **占位页面**——仅渲染 `<Empty description="历史记录功能将在后续版本中开放" />` |
| `HistoryPanel.jsx` | **功能完备**——已实现 `/api/history` 列表、日期/类型筛选、分页、点击加载详情 |
| `App.jsx:63` | 路由 `/history` → `<History />` 已注册 |
| `App.jsx:39-41` | 导航菜单 "分析历史" 已链接到 `/history` |

**US-HistoryPage 需求：** `History.jsx` 落地为完整页面，复用或重写 HistoryPanel 功能，展示分析历史（列表 + 详情），不再显示占位 Empty。

### 1.4 HistoryPanel 现有能力盘点

| 能力 | 实现状态 |
|------|---------|
| API 列表查询 (`GET /api/history`) | ✅ 已实现（分页 + 筛选） |
| 时间范围筛选 (RangePicker) | ✅ 已实现 |
| 漏洞类型筛选 (Select) | ✅ 已实现 |
| 分页 (Pagination) | ✅ 已实现（支持 10/20/50 页） |
| 加载详情 (`GET /api/history/{task_id}`) | ✅ 已实现 |
| 加载态 (Spin) | ✅ 已实现 |
| 错误态 (Alert + 重试) | ✅ 已实现 |
| 空态 (Empty) | ✅ 已实现 |
| localStorage 保存 (InputSection 侧) | ✅ 已实现（作为辅助存储） |
| 详情加载 → Redux dispatch | ✅ 已实现（setManualVulnerabilities / setChecklist / setTaskId） |

---

## 二、US-47: URL 分析模式 — 测试用例

### 2.1 后端 API 测试

#### TC-47-01: URL 分析基本请求

| 项目 | 内容 |
|------|------|
| **优先级** | P0 (阻塞) |
| **前置条件** | 后端服务运行中，AI 服务可用 |
| **测试步骤** | 1. 发送 `POST /analyze/url`<br>2. Body: `{"url": "https://example.com", "scan_depth": "basic"}` |
| **预期结果** | 返回 `code: 200`，`data` 包含 `vulnerabilities` 数组、`summary` 字符串、`cvss_overall` 数值、`task_id` 整数 |
| **验证点** | ✅ HTTP 200<br>✅ data.vulnerabilities 非空数组（若 AI 发现漏洞）<br>✅ data.summary 非空字符串<br>✅ data.task_id > 0<br>✅ 记录已写入 MySQL (AnalysisTask + Vulnerability)<br>✅ Redis 缓存已写入 |

#### TC-47-02: URL 格式校验

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 后端服务运行中 |
| **测试步骤** | 发送 `POST /analyze/url` 带无效 URL：<br>① `{"url": ""}`<br>② `{"url": "not-a-url"}`<br>③ `{"url": "ftp://invalid-protocol.com"}`<br>④ 不传 `url` 字段 |
| **预期结果** | 返回 `422 Unprocessable Entity` 或 `400 Bad Request`，含明确错误消息 |
| **验证点** | ✅ 空字符串被拒绝<br>✅ 非 URL 格式被拒绝<br>✅ 不支持的协议被拒绝或警告<br>✅ 缺失必填字段返回 422 |

#### TC-47-03: URL 分析可选参数

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 后端服务运行中 |
| **测试步骤** | 发送带可选参数的请求：<br>① `{"url": "...", "scan_depth": "deep"}`<br>② `{"url": "...", "include_headers": {"Authorization": "Bearer xxx"}}`<br>③ `{"url": "...", "target_paths": ["/api", "/admin"]}` |
| **预期结果** | 参数被正确传递到 AI 服务，请求成功（200） |
| **验证点** | ✅ 额外参数不影响基本流程<br>✅ 未知参数被忽略或警告（不崩溃） |

#### TC-47-04: URL 不可达处理

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 后端服务运行中 |
| **测试步骤** | 发送 `POST /analyze/url` 带不可达 URL：<br>① `"https://192.0.2.1"` (TEST-NET，不存在的地址)<br>② `"https://this-domain-does-not-exist-12345.com"` |
| **预期结果** | 后端应优雅处理：返回 200 但 data 中标注连接失败，或返回适当的错误码。**不应 500 崩溃** |
| **验证点** | ✅ 不 500 崩溃<br>✅ 返回有意义的错误信息<br>✅ 超时处理（非无限等待） |

#### TC-47-05: AI 服务不可用时的降级

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 后端服务运行中，模拟 AI 服务不可用 |
| **测试步骤** | 断开 AI 服务后发送请求 |
| **预期结果** | 返回 `503 Service Unavailable`，消息 "AI 服务暂时不可用" |
| **验证点** | ✅ HTTP 503（非 500）<br>✅ 错误消息明确<br>✅ 不在 DB 写入无效记录 |

#### TC-47-06: URL Schema 定义

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | schemas.py 已更新 |
| **测试步骤** | 检查 `UrlAnalysisRequest` Pydantic Schema |
| **预期结果** | 包含字段：`url: str`（必填，HttpUrl 类型）、`scan_depth: Optional[str]`、`model: Optional[str]` 等 |
| **验证点** | ✅ url 字段为 HttpUrl 类型且有验证<br>✅ scan_depth 有合法值约束（如 `basic`/`deep`）<br>✅ model 为 Optional |

---

### 2.2 前端 UI 测试

#### TC-47-07: URL Tab 可用性

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 前端已更新 |
| **测试步骤** | 1. 在 Dashboard 页面点击 "URL 扫描" Tab<br>2. 观察 Input 组件状态<br>3. 观察是否有提交按钮 |
| **预期结果** | — URL 输入框未被 `disabled`<br>— 无 "即将上线" 蓝色提示 banner<br>— 存在提交按钮（如 "开始 URL 分析"） |
| **验证点** | ✅ Input 可输入文本<br>✅ placeholder 有意义（如 "https://example.com"）<br>✅ 提交按钮可见且可点击<br>✅ 无 "即将上线" 文案 |

#### TC-47-08: URL 输入前端校验

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | URL Tab 可用 |
| **测试步骤** | 1. 不输入 URL → 点击提交<br>2. 输入无效 URL（如 "abc"）→ 点击提交<br>3. 输入有效 URL（如 "https://example.com"）→ 点击提交 |
| **预期结果** | ① 显示 "请输入目标 URL" 校验错误<br>② 显示 "请输入有效的 URL" 校验错误<br>③ 校验通过，发起 API 请求 |
| **验证点** | ✅ 空值校验<br>✅ URL 格式校验（前端先行）<br>✅ 有效 URL 通过校验 |

#### TC-47-09: URL 分析加载态

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | URL 输入有效 |
| **测试步骤** | 1. 输入有效 URL → 点击提交<br>2. 观察提交按钮状态 |
| **预期结果** | 提交按钮变为 loading 态（spinner + "分析中..."），不可重复点击 |
| **验证点** | ✅ 按钮显示 loading spinner<br>✅ 按钮 disabled 防止重复提交<br>✅ Input 在分析期间可设为 disabled 或保持不变 |

#### TC-47-10: URL 分析成功反馈

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 后端返回 200 |
| **测试步骤** | 提交 URL 分析并等待完成 |
| **预期结果** | — 显示 `notification.success` 提示分析完成<br>— 分析结果渲染到右侧 AIDetailAnalysis 区域<br>— 记录出现在 HistoryPanel 列表中 |
| **验证点** | ✅ 成功 Notification 弹出<br>✅ 漏洞列表可见<br>✅ 综合评分可见<br>✅ 历史列表新增一条记录 |

#### TC-47-11: URL 分析错误反馈

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 模拟后端返回错误（503/500/网络断连） |
| **测试步骤** | 提交 URL 分析，后端返回错误 |
| **预期结果** | — 显示 `notification.error` 含具体错误消息<br>— 按钮恢复正常状态<br>— 用户可重新提交 |
| **验证点** | ✅ 错误 Notification 弹出<br>✅ 错误消息可读<br>✅ 按钮恢复可点击<br>✅ 无白屏/崩溃 |

#### TC-47-12: Tab 切换状态保持

| 项目 | 内容 |
|------|------|
| **优先级** | P2 |
| **前置条件** | URL Tab 已输入内容 |
| **测试步骤** | 1. 在 URL Tab 输入 URL<br>2. 切换到 "手动描述" Tab<br>3. 切回 "URL 扫描" Tab |
| **预期结果** | URL 输入框内容可能保留或清空（取决于设计），但**不应崩溃** |
| **验证点** | ✅ 切换不报错<br>✅ UI 无闪烁/布局抖动<br>✅ 若设计为保留输入，内容仍在 |

---

### 2.3 集成测试

#### TC-47-13: URL 分析端到端流程

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 全部服务运行中 |
| **测试步骤** | 1. 前端 Dashboard → URL 扫描 Tab<br>2. 输入有效测试 URL<br>3. 点击提交<br>4. 等待分析完成<br>5. 查看结果<br>6. 切换至 HistoryPanel → 确认新记录存在<br>7. 在历史中点击加载该记录 |
| **预期结果** | 全链路通过：输入 → API → AI 分析 → 结果展示 → 历史存储 → 历史回看 |
| **验证点** | ✅ 步骤 1-7 全通过<br>✅ DB 中 AnalysisTask.input_type = "url"<br>✅ Redis 缓存存在且可命中<br>✅ 历史回看能正确还原结果 |

#### TC-47-14: URL 分析与已有模式并存

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 全部服务运行中 |
| **测试步骤** | 1. 提交一次手动描述分析<br>2. 提交一次 URL 分析<br>3. 提交一次文件上传分析<br>4. 查看 HistoryPanel |
| **预期结果** | 三种模式共存在历史列表中，`input_type` 分别为 "manual"、"url"、"file" |
| **验证点** | ✅ 三种 input_type 正确区分<br>✅ 每种均可独立加载详情<br>✅ 筛选功能不受影响 |

---

## 三、US-HistoryPage: /history 独立页面 — 测试用例

### 3.1 页面基本渲染

#### TC-HP-01: 页面路由可达

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 前端已更新 |
| **测试步骤** | 1. 浏览器访问 `/history`<br>2. 点击导航菜单 "分析历史" |
| **预期结果** | 页面正常渲染（非 404），不再显示 Empty 占位提示 |
| **验证点** | ✅ 路由匹配 `/history`<br>✅ 导航菜单高亮 "分析历史"<br>✅ 页面有实际内容（非占位 Empty） |

#### TC-HP-02: 历史列表加载

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | DB 中有历史分析记录 |
| **测试步骤** | 进入 `/history` 页面 |
| **预期结果** | 显示历史分析记录列表，每条包含：任务编号、输入类型、漏洞数量、CVSS 评分、创建时间 |
| **验证点** | ✅ 列表非空<br>✅ 每条记录有 id / input_type / vuln_count / cvss_overall / created_at<br>✅ 记录按时间倒序排列 |

#### TC-HP-03: 历史列表加载态

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 页面首次加载或网络较慢 |
| **测试步骤** | 进入 `/history` 页面 |
| **预期结果** | 加载期间显示 Spin/Loading 指示器，数据到达后平滑切换为列表 |
| **验证点** | ✅ 加载中有 spinner<br>✅ 加载完成后 spinner 消失<br>✅ 无布局跳动 |

#### TC-HP-04: 历史列表空态

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | DB 中无历史记录 |
| **测试步骤** | 清空历史后进入 `/history` 页面 |
| **预期结果** | 显示空态提示（Empty 组件），引导用户进行首次分析 |
| **验证点** | ✅ Empty 组件渲染<br>✅ 提示文案友好（如 "暂无分析记录，去创建第一条分析"）<br>✅ 可能有跳转到 Dashboard 的链接/按钮 |

#### TC-HP-05: 历史列表错误态

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 模拟 API 错误（断网 / 后端 500） |
| **测试步骤** | 进入 `/history` 页面时 API 返回错误 |
| **预期结果** | 显示错误 Alert，含错误消息 + 重试按钮 |
| **验证点** | ✅ Alert 组件显示错误信息<br>✅ "重试" 按钮存在且可点击<br>✅ 点击重试后重新请求 |

---

### 3.2 筛选功能

#### TC-HP-06: 时间范围筛选

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | DB 中有多日记录 |
| **测试步骤** | 1. 选择开始日期和结束日期（RangePicker）<br>2. 点击查询<br>3. 观察列表变化 |
| **预期结果** | 仅显示所选日期范围内的记录 |
| **验证点** | ✅ RangePicker 可选日期<br>✅ 列表结果限制在日期范围内<br>✅ 总数计数更新<br>✅ 清空日期后恢复全部 |

#### TC-HP-07: 漏洞类型筛选

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | DB 中有不同类型漏洞的记录 |
| **测试步骤** | 1. 选择 "SQLi" 类型<br>2. 点击查询<br>3. 切换为 "XSS" |
| **预期结果** | 仅显示包含该类型漏洞的任务 |
| **验证点** | ✅ 下拉列表包含所有类型选项（SQLi/XSS/SSRF/RCE/LFI/CSRF/XXE/Auth）<br>✅ 过滤结果正确<br>✅ "全部类型" 恢复全部记录 |

#### TC-HP-08: 组合筛选

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | DB 中有多条记录 |
| **测试步骤** | 同时设置时间范围 + 漏洞类型 → 点击查询 |
| **预期结果** | 结果同时满足两个条件（AND 逻辑） |
| **验证点** | ✅ 组合筛选结果正确<br>✅ 不互相覆盖 |

#### TC-HP-09: 筛选条件持久化

| 项目 | 内容 |
|------|------|
| **优先级** | P2 |
| **前置条件** | 页面显示筛选结果 |
| **测试步骤** | 1. 设置筛选条件并查询<br>2. 点击加载某条记录<br>3. 返回列表 |
| **预期结果** | 筛选条件保持（不重置为默认）或按设计行为明确重置 |
| **验证点** | ✅ 筛选条件不意外丢失<br>✅ 若重置需有明确提示 |

---

### 3.3 分页功能

#### TC-HP-10: 默认分页

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | DB 中有超过 20 条记录 |
| **测试步骤** | 进入 `/history` 页面 |
| **预期结果** | — 默认显示第 1 页，每页 20 条<br>— 底部显示分页组件，标注 "共 N 条" |
| **验证点** | ✅ 默认 page=1, page_size=20<br>✅ 分页组件可见<br>✅ 总数统计正确 |

#### TC-HP-11: 翻页

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 有多页数据 |
| **测试步骤** | 点击第 2 页、第 3 页 |
| **预期结果** | 列表内容正确切换，当前页码高亮，API 请求带正确 offset |
| **验证点** | ✅ 翻页后数据正确<br>✅ 页码高亮正确<br>✅ API 参数 page 正确递增 |

#### TC-HP-12: 每页条数切换

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | DB 中有足够记录 |
| **测试步骤** | 将每页条数从 20 切换为 50 |
| **预期结果** | 列表展示 50 条，跳到第 1 页，分页组件更新 |
| **验证点** | ✅ 选项 10/20/50 可用<br>✅ 切换后 page 重置为 1<br>✅ page_size=50 生效 |

#### TC-HP-13: 越界页码处理

| 项目 | 内容 |
|------|------|
| **优先级** | P2 |
| **前置条件** | 共 2 页数据 |
| **测试步骤** | 手动修改 URL 参数 page=999 → 发起查询 |
| **预期结果** | 返回空列表或自动纠正到最后一页，不报错 |
| **验证点** | ✅ 不返回 500/崩溃<br>✅ 显示空结果或合理处理 |

---

### 3.4 历史详情

#### TC-HP-14: 加载历史详情

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 列表中有历史记录 |
| **测试步骤** | 点击某条记录的 "加载" 按钮 |
| **预期结果** | — 调用 `GET /api/history/{task_id}`<br>— 详情数据加载到分析视图（vulnerabilities + checklist）<br>— 按钮显示 loading 态 |
| **验证点** | ✅ API 请求发送<br>✅ 漏洞列表正确渲染<br>✅ checklist 正确渲染<br>✅ 按钮 loading → 恢复正常 |

#### TC-HP-15: 详情加载错误处理

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 模拟详情 API 返回错误 |
| **测试步骤** | 点击某条记录的 "加载" |
| **预期结果** | 错误被静默 log 到控制台，用户可重试，不影响列表 |
| **验证点** | ✅ 不弹出全局错误<br>✅ 按钮恢复正常状态<br>✅ 列表不受影响 |

#### TC-HP-16: 详情包含 Checklist

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 历史任务包含 checklist 数据 |
| **测试步骤** | 加载一条有 checklist 的历史记录 |
| **预期结果** | checklist 在右侧面板正确渲染，包含 id / item_text / is_completed 字段 |
| **验证点** | ✅ checklist 项目可见<br>✅ is_completed 状态正确<br>✅ 无 is_completed 字段时的向前兼容 |

#### TC-HP-17: 缓存命中加速

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 某任务已缓存到 Redis |
| **测试步骤** | 1. 加载详情（首次，DB 查询）<br>2. 再次加载同一记录 |
| **预期结果** | 第二次响应 `from_cache: true`，速度明显快于首次 |
| **验证点** | ✅ 缓存命中标志 `from_cache: true`<br>✅ 缓存数据与 DB 一致<br>✅ 缓存 TTL 合理（非永久） |

---

### 3.5 页面布局与响应式

#### TC-HP-18: 页面布局完整性

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 页面已实现 |
| **测试步骤** | 进入 `/history` 页面 |
| **预期结果** | 页面包含：标题区（"分析历史"）、筛选区（类型 + 日期范围 + 查询按钮）、列表区、分页区。布局清晰 |
| **验证点** | ✅ 标题可见<br>✅ 筛选控件可用<br>✅ 列表占主要区域<br>✅ 分页在底部 |

#### TC-HP-19: 响应式适配 (Mobile)

| 项目 | 内容 |
|------|------|
| **优先级** | P2 |
| **前置条件** | 页面已实现 |
| **测试步骤** | 浏览器宽度缩小至 375px（手机尺寸） |
| **预期结果** | 筛选区垂直排列，列表正常显示，无横向滚动，分页组件适配小屏 |
| **验证点** | ✅ 无横向溢出<br>✅ 控件可点击/tap<br>✅ 内容可读 |

#### TC-HP-20: 与导航状态同步

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 页面已实现 |
| **测试步骤** | 1. 从 Dashboard 点击导航菜单 "分析历史"<br>2. 从 History 页面点击 "漏洞分析" 回到 Dashboard |
| **预期结果** | 导航高亮正确切换，页面内容正确渲染 |
| **验证点** | ✅ 导航菜单高亮对应项<br>✅ 无路由冲突<br>✅ 浏览器前进/后退正常 |

---

### 3.6 集成测试

#### TC-HP-21: Dashboard 内嵌 HistoryPanel 与独立 History 页面一致性

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | 全部服务运行中 |
| **测试步骤** | 1. 在 Dashboard 的 HistoryPanel 中查看历史列表<br>2. 跳转到 `/history` 独立页面<br>3. 对比两个列表 |
| **预期结果** | 两个视图查询同一 API，数据完全一致；筛选/分页行为一致 |
| **验证点** | ✅ Dashboard HistoryPanel 与 History 页面数据同步<br>✅ 筛选行为一致<br>✅ 分页行为一致<br>✅ 不重复请求（各自独立请求 OK） |

#### TC-HP-22: 新的分析反映在历史页面

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 全部服务运行中 |
| **测试步骤** | 1. 在 Dashboard 完成一次手动分析<br>2. 导航到 `/history` 页面<br>3. 确认新记录出现在列表顶部 |
| **预期结果** | 新分析记录立即出现在历史列表首页顶部 |
| **验证点** | ✅ 记录 `input_type` 正确<br>✅ 记录时间最新<br>✅ 可加载详情 |

---

## 四、回归测试用例

### 4.1 现有分析模式不受影响

#### TC-RG-01: 手动描述分析 — 正常流程

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | Sprint 9 代码已部署 |
| **测试步骤** | 完整执行一次手动描述分析（输入 → 提交 → 等待 → 查看结果） |
| **预期结果** | 与 Sprint 8 基线行为一致，无退化 |
| **验证点** | ✅ 分析成功<br>✅ 结果正确<br>✅ 历史记录写入 |

#### TC-RG-02: 文件上传分析 — 正常流程

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | Sprint 9 代码已部署 |
| **测试步骤** | 上传 ZAP JSON 文件 → 解析 → 批量分析 |
| **预期结果** | 与 Sprint 8 基线行为一致 |
| **验证点** | ✅ 文件解析成功<br>✅ 批量分析完成<br>✅ 历史记录写入 |

#### TC-RG-03: 多源对比分析 — 正常流程

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | Sprint 9 代码已部署 |
| **测试步骤** | 执行一次多源对比分析（ZAP + Nmap） |
| **预期结果** | 与 Sprint 8 基线行为一致 |
| **验证点** | ✅ 交叉验证结果正确<br>✅ 历史记录 `input_type: "multi"` 正常 |

#### TC-RG-04: PDF 报告生成

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | Sprint 9 代码已部署 |
| **测试步骤** | 调用 `GET /report/{task_id}/pdf?mode=full` |
| **预期结果** | 返回有效 PDF 文件 |
| **验证点** | ✅ Content-Type: application/pdf<br>✅ 文件可打开<br>✅ 内容完整 |

#### TC-RG-05: 健康检查端点

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | Sprint 9 代码已部署 |
| **测试步骤** | `GET /health` |
| **预期结果** | `{"code": 200, "message": "ok", "data": {"status": "healthy"}}` |
| **验证点** | ✅ 200 响应<br>✅ 返回格式不变 |

#### TC-RG-06: HistoryPanel 在 Dashboard 中仍正常工作

| 项目 | 内容 |
|------|------|
| **优先级** | P0 |
| **前置条件** | Sprint 9 代码已部署 |
| **测试步骤** | 在 Dashboard 左侧栏操作 HistoryPanel（筛选、翻页、加载详情） |
| **预期结果** | 所有功能正常，无退化 |
| **验证点** | ✅ 列表加载<br>✅ 筛选<br>✅ 分页<br>✅ 加载详情<br>✅ Redux dispatch 正常 |

#### TC-RG-07: 页面路由完整性

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | Sprint 9 代码已部署 |
| **测试步骤** | 访问以下路由：`/` (Dashboard)、`/history`、不存在的路由 → 404 |
| **预期结果** | 三个路径均正确渲染对应组件 |
| **验证点** | ✅ `/` → Dashboard<br>✅ `/history` → History 页面<br>✅ `/random` → 404 或无匹配处理 |

---

## 五、非功能测试

#### TC-NF-01: History API 响应时间

| 项目 | 内容 |
|------|------|
| **优先级** | P1 |
| **前置条件** | 200 条历史记录 |
| **测试步骤** | `GET /api/history?page=1&page_size=20` |
| **预期结果** | 响应时间 < 500ms（p95） |
| **验证点** | ✅ Response time < 500ms<br>✅ 无 N+1 查询 |

#### TC-NF-02: URL 分析超时处理

| 项目 | 内容 |
|------|------|
| **优先级** | P2 |
| **前置条件** | AI 分析耗时较长 |
| **测试步骤** | 提交 URL 分析 |
| **预期结果** | 前端有合理超时机制（如 120s），超时后显示错误，不永久 pending |
| **验证点** | ✅ 前端超时处理<br>✅ 后端超时不导致连接泄漏 |

#### TC-NF-03: 并发安全

| 项目 | 内容 |
|------|------|
| **优先级** | P2 |
| **前置条件** | Sprint 9 代码已部署 |
| **测试步骤** | 同时发送 5 个 URL 分析请求 |
| **预期结果** | 每个请求独立处理，DB 记录不冲突，无死锁 |
| **验证点** | ✅ 5 个任务全部成功或部分排队<br>✅ task_id 不重复<br>✅ 无死锁日志 |

---

## 六、测试数据准备

### 6.1 URL 分析测试数据

```json
// 正常 URL
{"url": "https://httpbin.org/get", "scan_depth": "basic"}
{"url": "https://example.com", "scan_depth": "deep"}

// 边界 URL
{"url": "http://localhost:8080/api"}     // localhost
{"url": "https://192.168.100.10"}        // 内网 IP
{"url": "https://sub.domain.com:8443/path?q=1#frag"}  // 复杂 URL

// 无效 URL
{"url": ""}
{"url": "not-a-url"}
{"url": "ftp://files.example.com"}
```

### 6.2 历史数据预填充

```sql
-- 确保有多种 input_type 的历史记录用于测试
-- manual / file / url / multi 各至少 5 条
-- 数据应跨越至少 3 个不同日期用于日期筛选测试
```

---

## 七、验收标准汇总

| 用户故事 | 关键验收项 | 阻塞项 |
|---------|-----------|--------|
| US-47 URL 分析 | URL Tab 可用、`POST /analyze/url` 端点正常、分析结果展示、保存到历史 | TC-47-01, TC-47-07, TC-47-13 |
| US-HistoryPage | `/history` 页面有实际内容、列表/筛选/分页正常、与 Dashboard 内嵌版一致 | TC-HP-01, TC-HP-02, TC-HP-06, TC-HP-07, TC-HP-14, TC-HP-21 |
| 回归 | 现有 4 种分析模式 + PDF 报告 + HistoryPanel 无退化 | TC-RG-01 ~ TC-RG-07 |

> **验收决策规则：**  
> — P0 用例 100% 通过 → 可验收  
> — P0 未通过 → 退回修复  
> — P1 用例 ≥ 90% 通过 → 附带已知问题记录验收  
> — P1 < 90% → 需评估风险后决定

---

*文档版本: v1.0 | 创建时间: 2026-06-30 | 最后更新: 2026-06-30*
